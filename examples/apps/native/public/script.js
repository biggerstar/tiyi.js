setTimeout(() => {
  if (!window['__TI_APP__']) {
    console.error('未通过太乙框架方式加载该应用', window, location.href)
  }
}, 1000)

// console.log('src script ');


// history.pushState({a:1},'','#test1')
// history.pushState({a:2},'','#test2')
// history.replaceState({a:3},'','#test3')







